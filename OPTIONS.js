function options() {
	background(background_one);
	returnHome()
	cursor_sprite.position.x = mouseX;
	cursor_sprite.position.y = mouseY;
	cursor_sprite.width = 1;
	cursor_sprite.height = 1;
	
// 	fill("Red");
// 	textSize(40);
// 	text("10 BULLETS", width/2-500, height/2)
// 	choose_quick.width = width/2-200;
// 	choose_quick.height = height-200
// 	choose_quick.position.x = width/2-525;
// 	choose_quick.position.y = height/2
	
// 	text("60 SECONDS", width/2+500, height/2);
// 	choose_seconds.width = width/2-200;
// 	choose_seconds.height = height-200
// 	choose_seconds.position.x = width-500;
// 	choose_seconds.position.y = height/2;
// 	if (mouseIsPressed) {
// 		if (mouseButton == LEFT) {
			
// 			if (cursor_sprite.overlap(choose_quick)) {
// 				game_mode = 10;
// 			}

// 			if (cursor_sprite.overlap(choose_seconds)) {
// 				game_mode = 60;
// 			}
			
// 		}
// 	}
}